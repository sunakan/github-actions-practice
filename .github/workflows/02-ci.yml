name: 継続的インテグレーション
on: push

jobs:
  step01-02:
    name: すてっぷ０１－０２
    runs-on: ubuntu-latest
    steps:
      - name: チェックアウト
        uses: actions/checkout@v2.0.0
      - name: cd 02-ci && すてっぷ１
        run: cd 02-ci && make step-01
      - name: すてっぷ２
        run: cd 02-ci && make step-02
